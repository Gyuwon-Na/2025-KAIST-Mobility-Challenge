mpc_path_tracker:
  ros__parameters:
    # --- MPC 설정 ---
    horizon: 75
    dt: 0.01
    lookahead_base: 0.3
    lookahead_per_speed: 0.15
    # --- 가중치 설정 ---
    Q_pos: 2.4
    Q_heading: 1.2
    R_v: 0.0
    R_w: 0.2
    # --- 제약 조건 ---
    max_velocity: 2.0
    max_accel: 1.0
    max_angular_vel: 2.0
    # --- 차량 정보 ---
    wheelbase: 0.33
    vehicle_width: 0.15